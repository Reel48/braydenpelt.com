import type { ArtPiece } from "@/lib/types";

/**
 * Your art. Shape (image required — art is visual):
 *   { title, image, medium?, year?, note?, url? }
 * `image` is a path under /public, e.g. "/images/art/piece.jpg".
 */
export const art: ArtPiece[] = [];
