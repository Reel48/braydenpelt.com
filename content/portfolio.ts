import type { Project } from "@/lib/types";

/**
 * Your work. Shape:
 *   { title, summary, slug?, year?, role?, tags?, url?, repo?, image? }
 * `image` is a path under /public, e.g. "/images/portfolio/foo.png".
 * No image? A blue gradient placeholder renders automatically.
 */
export const projects: Project[] = [];
