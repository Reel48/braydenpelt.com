import type { SportEntry } from "@/lib/types";

/**
 * Sports you play / have played. Shape:
 *   { sport, role?, team?, period?, note?, achievements?, image? }
 */
export const sports: SportEntry[] = [];
