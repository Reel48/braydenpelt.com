import type { Show } from "@/lib/types";

/**
 * TV shows. Shape:
 *   { title, year?, rating?, note?, poster? }
 * (A live integration like Trakt or Serializd can be added later.)
 */
export const shows: Show[] = [];
